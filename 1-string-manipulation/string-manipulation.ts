/**
 * Returns "Hello, World!"
 */
export function helloWorld(): string {
  throw Error("Not yet implemented")
}

/**
 * Returns a greeting containing `name` in the following format:
 * "Hello, <name>!"
 *
 * @example
 * // returns "Hello, Peter!"
 * greet("Peter")
 */
export function greet(name: string): string {
  throw Error("Not yet implemented")
}

/**
 * Returns a greeting containing `name` in the following format:
 * "Hello, <name>!"
 *
 * Returns "Hello, You!" if no name is given
 *
 * @example
 * // returns "Hello, Peter!"
 * greet("Peter")
 *
 * @example
 * // returns "Hello, You!"
 * greet()
 */
export function advancedGreet(name: string = "You"): string {
  throw Error("Not yet implemented")
}

/**
 * Returns the input text in all upper case.
 *
 * @example
 * // returns "THIS IS AN EXAMPLE"
 * scream("This is an Example")
 */
export function scream(text: string): string {
  throw Error("Not yet implemented")
}

/**
 * Return the number of characters in a given string.
 *
 * @example
 * // returns 5
 * countCharacters("Hello")
 */
export function countCharacters(text?: string): number {
  throw Error("Not yet implemented")
}

export function markTheCenter(text: string): string {
  throw Error("Not yet implemented")
}

export function advancedCountCharacters(text: string): number {
  // Return the number of characters in text. Ignore any white space characters.
  throw Error("Not yet implemented")
}

export const removePopel = (text: string): string => {
  // My friend Theo likes to sneak in the word "Popel" a lot. We cannot
  // allow that and have to replace this word with "foo" instead.
  throw Error("Not yet implemented")
};

export const getTagInnerHTML = (
  htmlString: string,
  tagName: string,
): string => {
  // Return the text between the start and end tag. For this example assume that each tag only exists once.
  throw Error("Not yet implemented")
}
