{
  "name": "tstutor",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "test": "node --import tsx --test ./**/*.test.ts --test-reporter tap",
    "coverage": "node --import tsx --test ./**/*.test.ts --experimental-test-coverage --test-reporter=lcov"
  },
  "devDependencies": {
    "tsx": "^4.6.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
